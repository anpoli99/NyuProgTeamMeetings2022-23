#include <bits/stdc++.h>
using namespace std;

// For this problem, I'm only including the Fast I/O methods for the template.
// The time limit is pretty tight for this problem; 
// You will probably TLE if you don't use them.


int main(){
    // Desynnc the input and output streams; means it will not flush the output buffer
    // Saves a lot of time
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n, qr;
    cin >> n >> qr;

    // Feel free to put any preprocessing here

    while(qr-- > 0){
        // For C++ we don't need anything else for Fast I/O
        // But make sure you use '\n' instead of endl
        // endl flushes the output buffer, which is slow
        // for example you can write:
        // cout << "yes\n";
    }
    
}
