#include <bits/stdc++.h>
using namespace std;

// Input: N and A, where A is a vector of N integers
// Output: The minimum number of darts to pop all balloons
int baloni(int N, vector<int> A){
    //Your code here
    return 0;
}

int main(){
    int N;
    cin >> N;
    vector<int> A(N);
    for(int i = 0; i < N; i++){
        cin >> A[i];
    }
    cout << baloni(N,A) << endl;
}